class Leap
    setUp: ->
        return

    onSocketConnected: ->
        return

    onDeviceConnected: ->
        return

    onDeviceDisconnected: ->
        return

env = self.Wage ?= {}
env.Leap = Leap
