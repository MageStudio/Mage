class Leap
    init: ->
        return

    addListeners: ->
        return

    onSocketConnected: ->
        return

    onDeviceConnected: ->
        return

    onDeviceDisconnected: ->
        return

env = self.Wage ?= {}
env.Leap = Leap
